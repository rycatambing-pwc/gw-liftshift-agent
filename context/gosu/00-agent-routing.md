---
document: gosu-agent-routing
purpose: Decide which Gosu/InsuranceSuite reference module to load during code scanning
scope: Code analysis, retrieval routing, source lookup
---

# Agent Routing

## Always load

- `01-quick-mental-model.md`

## Trigger map

| Trigger in code                                                                                                                                                                                              | Load these files                                                                                       | Why                                                                               |
| ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------- |
| Unknown Gosu syntax,`var`, `uses`, `construct`, `typeis`, `typeof`, `\ x ->`, `?.`, `?:`                                                                                                                     | `language/02a-syntax-basics.md`                                                                        | Core syntax, null safety, type checking, enumerations, intervals                  |
| `==`, `===`, `typeis`, `as`, `.cast(`, `structural`, `IDimension`, `MonetaryAmount`, `@AutoCreate`, `@ShortCircuitingProperty`, `Coercions`                                                                  | `language/02b-operators-and-types.md`                                                                  | Operators, structural types, dynamic/expando, reflection, dimensions, annotations |
| `\ x ->`, `\->`, `block(`, `using (`, `try`, `catch`, `finally`, `IMonitorLock`                                                                                                                              | `language/02c-blocks-and-closures.md`                                                                  | Blocks, closures, resource cleanup, exception handling                            |
| `{1,2,3}`, `.where(`, `.map(`, `.sum(`, `.groupBy(`, `.sortBy(`, `*.`, `+=` on array                                                                                                                         | `language/02d-collections.md`                                                                          | Collection creation and iterable operations                                       |
| `IDimension`, `MonetaryAmount`, `CurrencyAmount`, `unit(DateUtil`                                                                                                                                            | `language/02e-dimensions.md`                                                                           | Physical/financial quantities with unit arithmetic                                |
| `structure `, `&` compound type, structural assignability                                                                                                                                                    | `language/02f-structural-types.md`                                                                     | Capability-based typing without inheritance                                       |
| Java types,`getName()`→`.Name`, `uses java.lang.Math#`, Java interop                                                                                                                                         | `language/02g-java-interop.md`                                                                         | Java getter/setter mapping, static imports, generics reification                  |
| Entity classes,`PolicyPeriod`, `Policy`, `Account`, generated properties                                                                                                                                     | `platform/03-policycenter-runtime-model.md`, `platform/04-data-model-entity-metadata-and-typelists.md` | Entity behavior comes from metadata and runtime                                   |
| `.eti`, `.etx`, `.eix`, `foreignkey`, `array`, `edgeForeignKey`, `implementsEntity`, `addTo`, `removeFrom`, `setFieldValue`                                                                                  | `platform/04-data-model-entity-metadata-and-typelists.md`                                              | Entity metadata, relationships, and mutation rules                                |
| `.tti`, `.ttx`, `.tix`, `typekey`, `typefilter`, typecode                                                                                                                                                    | `platform/04-data-model-entity-metadata-and-typelists.md`                                              | Typelist and typekey semantics                                                    |
| `Bundle`, `Transaction`, `runWithNewBundle`, `newBundle`, `getCurrent`, `bundle.add`, `commit`, `isFieldChanged`, `getOriginalValue`, `entity.touch`, `ChangedBeans`                                         | `platform/10-bundles-and-transactions.md`                                                              | Writable/read-only bundle semantics, field change detection                       |
| `Query.make`, `compare`, `compareIn`, `Relop`, `join`, `subselect`, `select`                                                                                                                                 | `queries/05-query-format-proposal.md`, `queries/06-query-pattern-cards.md`                             | Query API shape and interpretation                                                |
| `where(` after `select()`, `firstWhere`, `Empty`, `FirstResult`, `getCountLimitedBy`, `withDistinct`, `between(`, `startsWith(`, `having(`, `setPageSize`, `orderBy(`, `thenBy(`, `intersect`, `ArrayLoader` | `queries/07-query-performance-antipatterns.md`, `queries/06-query-pattern-cards.md`                    | Query performance and DB vs memory distinction                                    |
| `ConnectionUtil`, `executeTransactionsWithReservedConnection`, `@WsiReduceDBConnections`, `ConnectionHandlerFactory`                                                                                         | `queries/30-db-connection-pool.md`                                                                     | DB connection pool reservation for multi-query operations                         |
| `.pcf`, `ListView`, `DetailView`, `InputSet`, `RowIterator`, `PanelRef`, `ListViewInput`, `ToolbarFilter`, `postOnChange`, `Client Reflection`, `validationExpression`                                       | `ui_and_rules/08-pcf-ui-model-and-embedded-gosu.md`                                                    | PCF context and embedded Gosu                                                     |
| `.gr`, `Preupdate`, `Validation`, `EventMessage`, `actions.exit`, `DisplayName`, `.en`, `Validatable`, `rejectField`, `triggersValidation`                                                                   | `ui_and_rules/09-rules-validation-and-entity-names.md`                                                 | Rules, validation, entity names                                                   |
| `enhancement`, `.gsx`, `_Ext`, `ScriptParameter`, `GosuDoc`, `deprecated`                                                                                                                                    | `oop/11a-naming-and-packages.md`, `oop/11b-enhancements.md`                                            | Enhancements and naming conventions                                               |
| `annotation `, `@AutoCreate`, `@AutoInsert`, `@ShortCircuitingProperty`, `@SuppressWarnings`, `@Target`, `@Retention`                                                                                        | `oop/11c-annotations.md`                                                                               | Annotation definitions and runtime access                                         |
| `delegate `, multi-interface delegation, composition pattern                                                                                                                                                 | `oop/11d-composition.md`                                                                               | Delegate-based composition                                                        |
| `_logger`, `Logger`, `StructuredLogger`, `CLF`, `contextMap`, `print(`, `DebugEnabled`, `ErrorEnabled`                                                                                                       | `cross_cutting/12-logging-and-structured-logger.md`                                                    | Logging review                                                                    |
| `gtest`, `GUnit`, `assertTrue`, `assertEquals`, `@Suites`, `TestServer`                                                                                                                                      | `cross_cutting/13-gunit-testing.md`                                                                    | Test file analysis                                                                |
| `Profiler.push`, `Profiler.pop`, `gwb inspect`, `DBCC`, `LockingLazyVar`, `LocklessLazyVar`, `Cache<`, `RequestVar`, `SessionVar`, `ThreadLocal`, `ReentrantLock`, `IMonitorLock`                            | `cross_cutting/14-system-health-profiler-dbcc-and-inspections.md`                                      | Performance, static analysis, concurrency primitives                              |
| `Extractable`, `archivingOwner`, `Overlap`, domain graph, archive graph                                                                                                                                      | `cross_cutting/15-archiving-and-domain-graph.md`                                                       | Archiving/domain graph                                                            |
| `FP64`, `fingerprint`, checksum, `toHexString`, `extend(new FP64`                                                                                                                                            | `cross_cutting/27-checksums-fingerprints.md`                                                           | FP64 fingerprint/checksum class                                                   |
| `.gsp`, `Gosu.RawArgs`, `gosu -e`, CLI program                                                                                                                                                               | `cross_cutting/28-gosu-programs-cli.md`                                                                | Standalone Gosu CLI programs                                                      |
| `XmlElement`, `QName`, XSD types, `$Children`, `$QName`, `$Text`, `XmlSerializationOptions`, `Base64Util`                                                                                                    | `integrations/xml-gosu.md`                                                                             | XML parsing, creation, serialization                                              |
| `Json.fromJson`, `Json.fromJsonUrl`, `toJson()`, `toStructure(`, `@ActualName`, `URL.JsonContent`                                                                                                            | `integrations/json-gosu.md`                                                                            | JSON parsing and structural typing                                                |
| `.gst`, `renderToString`, `render(writer`, `<%@ params`, `<%@ extends`, `<% `, `_gst`                                                                                                                        | `integrations/templates.md`                                                                            | Gosu template files                                                               |
| `dynamic.Dynamic`, `Expando`, `$getProperty`, `$invokeMethod`, `$getMissingProperty`, `IPlaceholder.UNHANDLED`                                                                                               | `integrations/dynamic-expando.md`                                                                      | Dynamic types and Expando objects                                                 |

## Project-source lookup rules

When a symbol is unresolved:

1. Check local `.gs` or `.gr` file.
2. Check imports/`uses`.
3. Check `.gsx` enhancements.
4. Check entity metadata: `.eti`, `.etx`, `.eix`.
5. Check typelists: `.tti`, `.ttx`, `.tix`.
6. Check PCF variables/root objects if inside `.pcf`.
7. Check generated Gosudoc/Javadoc/Data Dictionary.

## Do not retrieve by semantics alone

Use rule-based triggers for code symbols. Short tokens such as `#Field`, `Relop`, `typekey`, and `.gsx` may not retrieve well using embeddings alone.
